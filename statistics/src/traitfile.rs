

// #[derive(Debug)]
pub trait Calculate{
    fn calc(vector: Vec<i32>) -> f32{
        panic!("can't calculate mean")
    }
    // fn calc_mode(vector: Vec<i32>) -> i32{
    //     panic!("can't calculate mode")
    // }
    // fn calc_median(vector: Vec<i32>) -> i32{
    //     panic!("can't calculatae median")
    // }
}