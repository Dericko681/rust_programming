use std::{collections::{hash_map, HashMap}, vec};

use crate::traitfile::Calculate;

pub struct Mode {
    pub vector: Vec<i32>,
}

impl Calculate for Mode {

    fn calc(vector: Vec<i32>) -> f32{
        let ass_array = HashMap::new();
for i in vector{
    let mut counter = 0;
    for j in vec{
        if i == j{
            counter + 1;
            continue;
        }esle{
            r#continue;
        };

        j+1;
    }
ass_array.entry(i).or_insert(counter);
    i+1;
}
/* `f32` value */
    }
    
}